# Copy to /etc/bind-cache-relay/relay.env.
# Keep mode 0640 and owner root:relay.
NS_RELAY_TOKEN=SAME_LONG_RANDOM_TOKEN_AS_COLLECTOR
SMTP_USERNAME=REPLACE_SMTP_USERNAME
SMTP_PASSWORD=REPLACE_SMTP_PASSWORD
